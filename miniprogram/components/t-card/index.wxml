<view class="card {{compact ? 'card--compact' : ''}}">
  <view wx:if="{{title || extra}}" class="card__header">
    <view class="card__title">{{title}}</view>
    <view class="card__extra">{{extra}}</view>
  </view>
  <view class="card__body">
    <slot></slot>
  </view>
</view>
